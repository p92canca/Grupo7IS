// Direccion_unittest.cpp: Grupo7
// A sample program demonstrating using Google C++ testing framework.
//
// This sample shows how to write a more complex unit test for a class
// that has multiple member functions.
//
// Usually, it's a good idea to have one test for each method in your
// class.  You don't have to do that exactly, but it helps to keep
// your tests organized.  You may also throw in additional tests as
// needed.

//#include <fstream>
//#include <list>
/*#include <string>
#include "Direccion.h"
#include "gtest/gtest.h"

using namespace std;
using namespace Dentista;

TEST(Direccion, setCalleygetCalle) {
  Direccion d;  
  d.setCalle("Gran Capitan");
  EXPECT_EQ("Gran Capitan", d.getCalle());
}

TEST(Direccion, setCpygetCp) {
  Direccion d;
  d.setCp(14650);
  EXPECT_EQ(14650, d.getCp());
}

TEST(Direccion, setEscaleraygetEscalera) {
  Direccion d;
  d.setEscalera("A");
  EXPECT_EQ("A", d.getEscalera());
}

TEST(Direccion, setLocalidadygetLocalidad) {
  Direccion d;
  d.setLocalidad("Pozoblanco");
  EXPECT_EQ("Pozoblanco", d.getLocalidad());
}

TEST(Direccion, setNumeroygetNumero) {
  Direccion d;
  d.setNumero(14);
  EXPECT_EQ(14, d.getNumero());
}

TEST(Direccion, setPisoygetPiso) {
  Direccion d;
  d.setPiso('4');
  EXPECT_EQ('4', d.getPiso());
}

TEST(Direccion, setPuertaygetPuerta) {
  Direccion d;
  d.setPuerta('C');
  EXPECT_EQ('C', d.getPuerta());
}*/
